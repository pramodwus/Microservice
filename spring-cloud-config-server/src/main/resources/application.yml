server:
  port: 8888
  
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amresh087/spring-cloud-config-repository
          #user: amresh087
          #password: T
          search-paths:
          - zuul,client,server,eureka
          force-pull: true
          